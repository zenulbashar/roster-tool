/** Cookie holding the CSRF state nonce between connect and callback. */
export const OAUTH_STATE_COOKIE = "gdrive_oauth_state";

/** Cookie path — scoped to the integration routes only. */
export const OAUTH_COOKIE_PATH = "/api/integrations/google";
