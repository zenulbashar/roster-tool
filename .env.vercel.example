# ---------------------------------------------------------------------------
# Vercel — the Next.js WEB APP. Paste these into Vercel → Project → Settings →
# Environment Variables (Production). Same Neon database as the Railway worker.
# ---------------------------------------------------------------------------

# Neon Postgres connection — use the POOLED connection string (host contains
# "-pooler"), which is right for serverless. Must include ?sslmode=require.
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx-pooler.ap-southeast-2.aws.neon.tech/roster?sslmode=require

# Secret that signs owner sign-in sessions. Generate once with: npx auth secret
# Use the SAME value here and on the worker.
AUTH_SECRET=replace-with-a-long-random-string

# Canonical site URL Auth.js uses to build owner magic-link sign-in URLs.
AUTH_URL=https://roster.zaleit.com.au

# Public base URL used to build staff magic links and the shareable roster link.
APP_URL=https://roster.zaleit.com.au

# Send real email via Resend (not the local SMTP catcher).
EMAIL_TRANSPORT=resend

# Verified sender on your Resend domain (zaleit.com.au).
EMAIL_FROM=Roster <roster@zaleit.com.au>

# Resend API key with sending access.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx

# Cloudflare Turnstile — REQUIRED before publishing any form. The public
# form-fill route verifies the token server-side and fails closed in production
# if the secret is missing. Create a widget at dash.cloudflare.com > Turnstile.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAA_your_site_key
TURNSTILE_SECRET_KEY=0x4AAAAAAA_your_secret_key

# Inbound SSO from prompt2eat — the Ed25519 PUBLIC key (public PEM or its base64)
# that verifies handoff tokens at POST /api/sso/prompt2eat. OPTIONAL; the verify
# path fails closed when unset. prompt2eat holds the matching private key, so a
# Roster compromise can never mint a prompt2eat-trusted token. See
# docs/roster-sso-contract.md.
# PROMPT2EAT_SSO_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# Google Drive document storage (optional feature). Set all four to enable the
# owner "Connect Google Drive" flow; the connect flow stays disabled until they
# are all present. drive.file scope only. The redirect URI MUST match an
# authorized redirect URI on the OAuth client in Google Cloud Console. Generate
# the encryption key once with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
GOOGLE_CLIENT_ID=xxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxx
GOOGLE_OAUTH_REDIRECT_URI=https://roster.zaleit.com.au/api/integrations/google/callback
TOKEN_ENCRYPTION_KEY=base64-of-32-random-bytes

# Xero Payroll AU integration (optional feature). Set all three to enable the
# owner "Connect Xero" flow; it stays disabled until all three (plus the shared
# TOKEN_ENCRYPTION_KEY above) are present. Read/timesheet-write scopes only — the
# app has NO pay-run capability. The redirect URI MUST match a redirect URI on
# the Xero app. Pushes create DRAFT timesheets a human approves inside Xero.
# NOTE (first live AU connect): the AU-2.0 timesheet base path + scope are
# isolated in src/lib/xero/tokens.ts (XERO_TIMESHEET_BASE_PATH,
# XERO_TIMESHEET_SCOPE) and must be verified against a live AU demo company —
# see "Xero — live-verify checklist" in the README.
XERO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
XERO_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
XERO_OAUTH_REDIRECT_URI=https://roster.zaleit.com.au/api/integrations/xero/callback

# Production mode (Vercel usually sets this automatically; included for clarity).
NODE_ENV=production
